
  # Create note app layout

  This is a code bundle for Create note app layout. The original project is available at https://www.figma.com/design/L2R2gTEnEAxj52aWNIEdaF/Create-note-app-layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  